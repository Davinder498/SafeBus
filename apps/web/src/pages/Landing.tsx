import { Button, Card } from "@safebus/ui";
export function Landing() { const sections = ["Problem", "Solution", "Admin dashboard preview", "Driver app preview", "Parent bus visibility preview", "Privacy-first", "Pilot program", "Contact"]; return <main><section className="bg-slate-950 px-4 py-20 text-white"><div className="mx-auto max-w-6xl"><p className="font-bold text-yellow-300">SafeBus Alberta</p><h1 className="mt-4 max-w-4xl text-4xl font-black md:text-6xl">Real-time school bus visibility for Alberta schools and parents.</h1><p className="mt-5 max-w-3xl text-lg text-slate-300">SafeBus helps schools track active bus trips, notify parents, confirm pickup and drop-off events, and manage transportation exceptions from one simple platform.</p><div className="mt-8 flex gap-3"><a href="/login"><Button variant="secondary">Request Pilot</Button></a><a href="#how"><Button variant="ghost">See How It Works</Button></a></div></div></section><section id="how" className="mx-auto grid max-w-6xl gap-4 px-4 py-12 md:grid-cols-2">{sections.map((s) => <Card key={s}><h2 className="text-xl font-black">{s}</h2><p className="mt-2 text-slate-600">A focused transportation command center for active trips, exceptions, and privacy-safe bus visibility.</p>{s === "Privacy-first" ? <p className="mt-4 text-2xl font-black text-slate-950">Track the bus, not the child.</p> : null}</Card>)}</section></main>; }
