import { Link } from "react-router-dom";
import { Button, Card } from "@safebus/ui";
function AuthShell({ title, helper }: { title: string; helper: string }) { return <main className="grid min-h-screen place-items-center bg-slate-100 p-4"><Card className="w-full max-w-md"><h1 className="text-2xl font-black">{title}</h1><p className="mt-2 text-sm text-slate-500">{helper}</p><label className="mt-5 block text-sm font-bold">Email</label><input className="mt-1 w-full rounded-xl border p-3" placeholder="demo@example.invalid" /><div className="mt-5 grid gap-2"><Link to="/admin"><Button className="w-full">Continue as admin</Button></Link><Link to="/parent"><Button className="w-full" variant="ghost">Continue as parent</Button></Link><Link to="/driver-demo"><Button className="w-full" variant="secondary">Open driver demo</Button></Link></div><div className="mt-4 flex justify-between text-sm"><Link to="/forgot-password">Forgot password</Link><Link to="/reset-password">Reset password</Link></div></Card></main>; }
export function Login() { return <AuthShell title="Demo login" helper="Mock authentication only for Milestone 1 preview." />; }
export function ForgotPassword() { return <AuthShell title="Forgot password" helper="Placeholder recovery flow. No email is sent in Milestone 1." />; }
export function ResetPassword() { return <AuthShell title="Reset password" helper="Placeholder reset flow for UI preview only." />; }
